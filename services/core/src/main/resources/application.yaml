server:
  port: 8004

spring:
  application:
    name: core
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    schemas: core_schema
    default-schema: core_schema
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 55MB
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}

jwt:
  secret: ${JWT_SECRET:TTdGFPf9lbnQ3J3GBU8MnwQfp3oaawcBzf9LlBJpqlE=}

notes:
  attachments:
    storage-path: ${NOTE_ATTACHMENTS_PATH:./data/note-attachments}
