package com.lifeos.job_tracker.domains.enums;

/** Where a referral ask sits - from identifying a contact through to their reply. */
public enum ReferralStatus {
  NOT_CONTACTED,
  MESSAGE_DRAFTED,
  CONTACTED,
  RESPONDED,
  REFERRED,
  DECLINED
}
