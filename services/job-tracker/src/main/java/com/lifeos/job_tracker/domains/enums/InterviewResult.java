package com.lifeos.job_tracker.domains.enums;

/** Outcome of one interview round, set by the candidate once they know it. */
public enum InterviewResult {
  PENDING,
  PASSED,
  FAILED,
  CANCELLED
}
